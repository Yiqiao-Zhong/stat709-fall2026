import { mountDiscussion } from './discussion.js';

// Module scripts run after the document is parsed. The preface does not need
// lecture widgets, priority filters, or exercise navigation.
mountDiscussion();
